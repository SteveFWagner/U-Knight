module.exports = {

  submitForm: (req, res) => {
    // const db = req.app.get('db')
    // db.submitForm().then(resp => {
    //   res.status(200).send(resp)
    // })
    console.log('does stuff')
  }

}